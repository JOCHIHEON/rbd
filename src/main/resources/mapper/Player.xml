<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.PLAYER">
	<insert id="insertPlayer" parameterType="playerVo">
		insert into
		player_info(team_code, player_name, player_birth, player.height,
		player_position, player_background,player_number)
		values(#{team_code},
		#{player_name}, #{player_birth}, #{player.height}, #{player_position},
		#{player_background},#{player_number})
	</insert>
	<update id="updatePlayer" parameterType="playerVo">
		update player_info
		<set>
			<if test="team_code != null">team_code = #{team_code}</if>
			<if test="player_name != null">,player_name =#{player_name}</if>
			<if test="player_birth != null">, player_birth = #{player_birth}</if>
			<if test="player_height != null">, player_height = #{player_height}</if>
			<if test="player_position != null">, player_position = #{player_position}</if>
			<if test="player_number != null">, player_number = #{player_number}</if>
		</set>
		where player_no = #{player_no}
	</update>
	<delete id="deletePlayer" parameterType="playerVo">
		delete from player_info where player_no = #{player_no}
	</delete>
	
	<select id="playerView" parameterType="int">
		select * from player_info where player_no = #{player_no}
	</select>
	<select id="playerListView" resultType="playerVo" parameterType="playerVo">
		select * from player_info
	</select>
</mapper>
